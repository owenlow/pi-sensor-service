nav(class="navbar navbar-expand-lg navbar-light bg-light")
    a(class="navbar-brand" href="#") HermesSite
    button(class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation")
        span(class="navbar-toggler-icon")
    div(class="collapse navbar-collapse" id="navbarSupportedContent")
        ul(class="navbar-nav mr-auto")
            li(class="nav-item " + (currentPage === 'landing' ? 'active' : ''))
                a(class="nav-link" href="/") Home
            li(class="nav-item " + (currentPage === 'camera' ? 'active' : ''))
                a(class="nav-link" href="/camera") Photo
            li(class="nav-item " + (currentPage === 'pixels' ? 'active' : ''))
                a(class="nav-link" href="/pixels") Pixels